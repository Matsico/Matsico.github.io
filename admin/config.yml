backend:
  name: test-repo

media_folder: "images"
public_folder: "/images"

collections:
  - name: "test"
    label: "Test"
    folder: "data"
    create: true
    fields:
      - { label: "Titel", name: "titel", widget: "string" }

